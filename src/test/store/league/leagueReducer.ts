const initialState = {
    leagues: []
};

const leagueReducer = (state = initialState, action: string) => {
    switch (action) {
        default:
            return state;
    }
};

export default leagueReducer